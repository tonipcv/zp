import { Inter, Space_Grotesk } from 'next/font/google'

export const satoshi = Space_Grotesk({
  subsets: ['latin'],
  variable: '--font-satoshi',
  display: 'swap',
})

export const euclidCircular = Inter({
  subsets: ['latin'],
  variable: '--font-euclid',
  display: 'swap',
}) 